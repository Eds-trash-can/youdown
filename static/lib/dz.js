/* If i find enough small enhancments to js coding im creating dzjs or $$$js or whatever just an lib with some shit
  Its named cc
  for ...
  ehmmmmmmmmmmm..........
  creative........P????0???
  ah yes creation
  its named creative creation! (with an even more creative name!)

  const $$$ = new class {
    async ps(promise, tag) {
        promise.then((a) => {$(tag).html = a})
    }
}
*/
const cc = new class {
    constructor() {
        this.log = console.log
    }

    get clear() {
        console.clear()
    }
    if(cond, rettrue, retfalse) {
        return cond ? rettrue : retfalse
    }
}
const concatObjects = (...sources) => {
    const target = {};
    sources.forEach(el => {
       Object.keys(el).forEach(key => {
          target[key] = el[key];
       });
    });
    return target;
 }